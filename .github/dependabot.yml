version: 2
updates:
  # Keep the SHA-pinned actions in the dashboard workflow current. Dependabot
  # bumps both the pinned commit SHA and its trailing version comment.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
    labels:
      - dependencies
